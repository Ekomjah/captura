# API base path or URL for axios (see app/frontend/src/lib/api/client.ts).
# Leave empty to use the Vite dev proxy: /v1 -> http://localhost:8000 (make dev-api).
# Example direct URL: http://localhost:8000/v1
VITE_API_BASE_URL=
